# Redrob Candidate Ranker — Dependencies
#
# Ranking pipeline (rank.py):
#   numpy + sentence-transformers (hybrid semantic: TF-IDF all + neural top 500)
#   all-MiniLM-L6-v2 (~80 MB) downloaded once, cached locally.
#   No network calls during ranking itself.
#
# Sandbox app (app.py):
#   streamlit + pandas + plotly

# Core ranking
numpy>=1.24.0
sentence-transformers>=2.6.0

# Sandbox
streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.18.0

# Tests
pytest>=7.4.0
