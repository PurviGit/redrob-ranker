# Redrob Candidate Ranker v2 — Dependencies
#
# rank.py (ranking):
#   stdlib only: json, csv, gzip, argparse, re, math, pathlib, time
#   + numpy (scientific computing, pre-installed everywhere)
#
# Optional — neural semantic scoring (better accuracy):
#   pip install sentence-transformers
#   Downloads all-MiniLM-L6-v2 (~80 MB) on first run.
#   rank.py auto-detects and switches from TF-IDF to neural. No code change.
#
# Sandbox app (app.py):
#   pip install streamlit pandas plotly

numpy>=1.24.0

# Optional: uncomment for neural semantic scoring
# sentence-transformers>=2.6.0

# Sandbox
streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.18.0

# Tests
pytest>=7.4.0
